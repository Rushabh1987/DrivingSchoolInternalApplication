# Driving School Backend

FastAPI backend for the internal Driving School app.

## Local development

```bash
uv run uvicorn app.main:app --reload
```

The health endpoint is available at:

```text
http://localhost:8000/health
```
